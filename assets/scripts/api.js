
const getChatsApi = function(){
  console.log("we've reached the api for getting all chats....")
}

const sendMessageApi = function(){
  console.log("we've reached the api for sending messages....")
}

module.exports = {
  getChatsApi,
  sendMessageApi
}
